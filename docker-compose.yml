version: '3.0'
services:
  mongo:
    image: mongo:4.2
    container_name: "mongo"
    volumes:
      - mongo_data:/data/db
    restart: always

  shiny-eevee-bot:
    container_name: "fb"
    build: .
    depends_on:
      - mongo
    environment:
      - FOOBERRY_TOKEN=${FOOBERRY_TOKEN}
    restart: always

volumes:
  mongo_data: